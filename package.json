{
  "name": "next-mono",
  "private": true,
  "dependencies": {
    "next": "latest"
  },
  "workspaces": [
    "*"
  ],
  "scripts": {
    "dev": "yarn --cwd web dev",
    "build": "yarn --cwd web build",
    "start": "yarn --cwd web start"
  }
}
